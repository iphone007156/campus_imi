name: campus_imi
description: СВФУ Campus App for students

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  image_picker: ^1.2.3          # ← Версия из кэша
  firebase_core: ^3.15.2        # ← Версия из кэша (3.15.2)
  firebase_auth: ^5.7.0         # ← Версия из кэша (5.7.0)
  firebase_storage: ^12.4.10    # ← Версия из кэша (12.4.10)
  cloud_firestore: 5.6.12

flutter:
  uses-material-design: true
  assets:
    - assets/images/